# Talk-And-Task File Usage Map

Generated for the current repository state. The active app path is `src/main.jsx` -> `src/App.jsx` -> `src/components/ChatApp.jsx`. Files at the repository root that duplicate `src/` components appear to be legacy/orphan copies unless explicitly noted.

| File | Usage in app | Functions / config / data used and how it maps through the app |
|---|---|---|
| `.github/workflows/firebase-hosting-merge.yml` | GitHub Actions deploy workflow for Firebase Hosting after pushes. | Runs `npm install && npm run build`, then `FirebaseExtended/action-hosting-deploy@v0` to deploy live to Firebase project `niltask`. Trigger is `push` to `master`; the repo default branch is `main`, so this may not run unless `master` exists. |
| `.github/workflows/firebase-hosting-pull-request.yml` | GitHub Actions preview workflow for pull requests. | Runs the Vite build and publishes a Firebase Hosting preview channel for same-repo PRs using `FIREBASE_SERVICE_ACCOUNT_NILTASK`. |
| `.nojekyll` | Static-hosting marker. | Empty marker file that tells GitHub Pages not to process the site with Jekyll. Not used by React runtime. |
| `AdminEditUserModal.jsx` | Legacy/root modal copy; not reached from the active `src/main.jsx` import graph. | Exports `AdminEditUserModal`. UI for editing a user's name, approval/admin flags, and group creation permission. Active version is `src/components/Modals/AdminEditUserModal.jsx`. |
| `App.jsx` | Legacy/root app shell; not used by current Vite entry because `src/main.jsx` imports `src/App.jsx`. | Defines `FallbackScreen`, `App`, `SafeChatApp`. Handles Google sign-in, first-user/admin bootstrap, last login/logout updates, and renders `ChatApp` inside `ErrorBoundary`. Root copy imports `./components/...`, so it would only work if a root `components` folder existed. |
| `ContextMenuModal.jsx` | Legacy/root modal copy; not reached from active app. | Exports `ContextMenuModal`. Presents message actions such as reply, react, edit, delete, pin, bookmark, convert to task, and task-related actions. Active version is under `src/components/Modals`. |
| `ErrorBoundary.jsx` | Legacy/root error boundary; not reached from active app. | Class component catches render errors with `getDerivedStateFromError` and shows a reload fallback. Active version is `src/components/ErrorBoundary.jsx`. |
| `firebase.js` | Legacy/root Firebase config; not reached by active `src/` app. | Initializes Firebase and exports `auth`, `db`, `storage`, plus re-exported Auth/Firestore/Storage helpers. Active version is `src/firebase.js`. |
| `firebase.json` | Firebase Hosting config. | Sets `dist` as the public build directory, enables SPA fallback to `/index.html`, and ignores config/node_modules files during deploy. |
| `FILE_USAGE_MAP.md` | Documentation created for this request. | Three-column map of repository files, their role in the app, and the functions/config/data each file contributes to the runtime or deployment flow. Not imported by the app. |
| `GroupFormModal.jsx` | Legacy/root group creation/editing modal; not reached from active app. | Exports `GroupFormModal`. Captures group name, members, and optional group image. Active version is `src/components/Modals/GroupFormModal.jsx`. |
| `GroupSettingsModal.jsx` | Legacy/root group settings modal; not reached from active app. | Defines `GroupSettingsModal`, `handleImageChange`, `toggleMember`, `onSubmit`. Edits group name/avatar/members and calls parent update handlers. Active version is `src/components/Modals/GroupSettingsModal.jsx`. |
| `icon-192.png` | Static icon asset. | Used as a repository asset/icon. Note: `vite.config.js` PWA manifest references `/icon-192x192.png` and `/icon-512x512.png`, which are different names from this file. |
| `imageUtils.js` | Legacy/root image helper; not reached from active app. | Exports `compressImage(file, maxWidth, maxHeight, quality)`. Canvas-based image compression before upload. Active version is `src/utils/imageUtils.js`. |
| `index.css` | Legacy/root stylesheet; not imported by active app. | Contains global CSS/Tailwind-style rules for the older root layout. Active stylesheet is `src/index.css`. |
| `index.html` | Vite HTML entry. | Loads Font Awesome CSS, defines `<div id="root">`, and loads `/src/main.jsx`. This is the top browser entry for the app. |
| `LeftSidebar.jsx` | Legacy/root sidebar copy; not reached from active app. | Exports `LeftSidebar`. Lists groups, DMs, search, unread/task badges, and admin/profile/logout actions. Active version is `src/components/Sidebar/LeftSidebar.jsx`. |
| `package.json` | Project dependency and script manifest. | Scripts: `dev` starts Vite, `build` creates `dist`, `preview` serves the build. Dependencies include React, Firebase, jsPDF, Font Awesome, Tailwind, Vite, and PWA tooling. |
| `postcss.config.js` | Build pipeline config. | Enables Tailwind CSS and Autoprefixer for CSS processing during Vite builds. |
| `ProfileSettingsModal.jsx` | Legacy/root profile settings modal; not reached from active app. | Exports `ProfileSettingsModal`; defines `handleSoundPreview`. Edits name, avatar, notification sound profile, and tool preferences. Active version is `src/components/Modals/ProfileSettingsModal.jsx`. |
| `PROJECT_STATE.md` | Human project notes. | Documents current architecture, important Firebase import rule, known priorities, and component boundaries. Not imported by the runtime. |
| `ReminderModal.jsx` | Legacy/root reminder modal; not reached from active app. | Exports `ReminderModal`. Collects reminder datetime and calls parent `setReminder`. Active version is `src/components/Modals/ReminderModal.jsx`. |
| `RightSidebar.jsx` | Legacy/root details sidebar; not reached from active app. | Exports `RightSidebar`. Shows group members, pinned messages, task summaries, task board/admin switching. Active version is `src/components/Sidebar/RightSidebar.jsx`. |
| `ScheduleSendModal.jsx` | Legacy/root scheduled-send modal; not reached from active app. | Exports `ScheduleSendModal`. Collects send datetime and calls parent scheduler. Active version is `src/components/Modals/ScheduleSendModal.jsx`. |
| `service-worker.js` | Standalone service worker file. | Implements install/activate/fetch caching behavior for static assets and navigation fallback. The active PWA config in `vite.config.js` also generates Workbox behavior. |
| `tailwind.config.js` | Tailwind theme config. | Scans `index.html` and `src/**/*.{js,ts,jsx,tsx}`. Defines app colors: primary, surface, panel, text, border, success, danger, and warning. |
| `TaskAnalyticsModal.jsx` | Legacy/root task analytics modal; not reached from active app. | Exports `TaskAnalyticsModal`. Shows task totals, completion rate, overdue count, and daily created/completed bars. Active version is `src/components/Modals/TaskAnalyticsModal.jsx`. |
| `TaskConvertModal.jsx` | Legacy/root convert-to-task modal; not reached from active app. | Exports `TaskConvertModal`. Captures task title, assignees, priority, deadline, acknowledgment requirement, and proof requirement. Active version is `src/components/Modals/TaskConvertModal.jsx`. |
| `TaskTrailModal.jsx` | Legacy/root task trail modal; not reached from active app. | Exports `TaskTrailModal`. Shows task metadata, trail updates, comments, attachments, delegation, and completion actions. Active version is `src/components/Modals/TaskTrailModal.jsx`. |
| `UploadOverlay.jsx` | Legacy/root upload progress overlay; not reached from active app. | Exports `UploadOverlay`. Displays current upload filename and percent progress. Active version is `src/components/Common/UploadOverlay.jsx`. |
| `vercel.json` | Vercel SPA routing config. | Rewrites all paths to `/index.html`, allowing client-side routes to load correctly on refresh. |
| `vite.config.js` | Vite/PWA build config. | Calls `defineConfig`, `react()`, and `VitePWA()`. Enables auto-updating PWA service worker, Google APIs network-only runtime handling, manifest metadata, and jsPDF dependency optimization. |
| `src/App.jsx` | Active authentication shell for the app. | `FallbackScreen` renders crash UI. `App` observes Firebase Auth, handles Google login, creates/updates Firestore user docs, records login/logout timestamps, and gates between loading, login, and the chat app. `SafeChatApp` wraps `ChatApp` render and reports synchronous crashes. Uses Firebase helpers from `src/firebase.js` plus direct Firestore imports. |
| `src/firebase.js` | Active Firebase initialization module. | Initializes Firebase app and exports `auth`, `db`, `storage`, plus selected Auth/Firestore/Storage helpers used by `src/App.jsx`, `ChatApp`, hooks, and modals. Central connection point for Google Auth, Firestore data, and Storage uploads. |
| `src/index.css` | Active global stylesheet. | Imports Tailwind layers and defines app-wide visual behavior: scrollbars, theme colors, animations, message/task styling, sidebar responsiveness, modal polish, mobile safe-area behavior, and rich text/content styles. |
| `src/main.jsx` | Active React entry point. | Imports React, `ReactDOM.createRoot`, `App`, and `src/index.css`; mounts `<App />` inside `<React.StrictMode>` at `#root` from `index.html`. |
| `src/components/Admin/AdminPanel.jsx` | Admin dashboard inside the app. | `stripHtml` cleans message/task text. `AdminPanel` renders overview, users, groups, tasks, audit, SLA, and system health views. `generatePDFHeaderFooter`, `exportOverviewPDF`, `exportGroupsPDF`, `exportTasksPDF` create downloadable reports. `getStatusColor` and `getPriorityColor` style task badges. Used by `ChatApp` when admin view is selected. |
| `src/components/ChatApp.jsx` | Main application coordinator. | `ThreadSidebar` renders reply threads. `ChatApp` owns most UI state and wires data hooks, chat, sidebars, modals, uploads, tasks, reminders, scheduling, notifications, admin mode, search, and profile/group updates. Key handlers include `handleSendMessage`, `handleSaveEdit`, `handleSendPendingFiles`, `handleFileUpload`, `handlePaste`, `handleScheduleMessage`, `convertToTask`, `handleDelegateTask`, `handleCompleteTask`, `handleAddComment`, `handleTrailFileUpload`, `setReminder`, `handleEditUserSubmit`, `handleReactionIntercept`, `handleWipeAllTasks`, `handleGroupPicUpload`, `handleUpdateGroupMembers`, `handleGroupSubmit`, `onGroupUpdate`, `handleProfileSubmit`, `getUnreadInfoForUser`, and `getUnreadInfoForGroup`. Imports almost every active UI module and passes callbacks/data downward. |
| `src/components/Chat/ChatView.jsx` | Scrollable chat transcript. | `ChatView` receives filtered messages from `ChatApp`, renders date separators, sender avatars, unread highlights, typing indicators, and `MessageBubble` rows. `handleChatScroll` supports upward infinite/older-message loading by calling `loadMoreMessages`. |
| `src/components/Chat/InputArea.jsx` | Message composer and command toolbar. | `InputArea` handles rich text input, mentions, emoji picker, selected file previews, quick task/schedule actions, and send button behavior. `handleInput` syncs editable HTML to parent state. `handleSelectMention` inserts user/group mentions. `checkSelection` tracks selected text. `insertEmoji` inserts emoji at the caret. Used by `ChatApp`. |
| `src/components/Chat/MessageBubble.jsx` | Individual message/task renderer. | `stripHtml` sanitizes text for previews. `MessageBubble` renders normal messages, files, private mentions, reactions, bookmarks, pins, task cards, inline task actions, and thread/task controls. `getBorderColor` styles task priority/status. `handleAcknowledge`, `handleInlineSaveTitle`, `submitInlineUpdate`, `handleInlineComplete`, `handleInlineDelegateSubmit`, and `handleInlineFileUpload` update Firestore task fields/trail and Storage attachments. Used by `ChatView`, thread view, and some notification/task contexts. |
| `src/components/Common/MemoizedAvatar.jsx` | Shared avatar component. | `MemoizedAvatar` is wrapped in `React.memo` and uses `useMemo` to derive initials from names. It displays user/group images when present and stable fallback initials when absent. Used throughout sidebars, chat, admin, task boards, and modals. |
| `src/components/Common/Toast.jsx` | Toast notification stack. | `Toast` renders temporary success/error/info messages and uses `useEffect` timers to call `removeToast`. Driven by `ChatApp` state. |
| `src/components/Common/UploadOverlay.jsx` | Upload progress overlay. | `UploadOverlay` displays `fileName` and `uploadProgress`. Used by `ChatApp` and also exposed through `ModalManager` for active uploads. |
| `src/components/ErrorBoundary.jsx` | Active React error boundary. | Class component catches child render errors, stores them in state, and displays a compact reload screen. Used by `src/App.jsx` around `SafeChatApp`. |
| `src/components/Modals/ActiveSchedulesModal.jsx` | Modal for scheduled messages and active reminders. | `ActiveSchedulesModal` loads pending scheduled messages from Firestore, combines them with pending reminders, and allows delete/edit/update operations. Uses `collection`, `query`, `where`, `getDocs`, `deleteDoc`, `doc`, and `updateDoc`. Opened through `ModalManager`. |
| `src/components/Modals/AdminEditUserModal.jsx` | Admin user-edit modal. | `AdminEditUserModal` renders editable fields for selected user admin form and calls `handleEditUserSubmit` from `ChatApp`. Opened through `ModalManager`. |
| `src/components/Modals/ContextMenuModal.jsx` | Message action modal. | `ContextMenuModal` conditionally renders action buttons based on selected message type, permissions, reactions, and task state. Calls parent handlers for reply, react, edit, delete, pin, bookmark, convert to task, trail, delegate, complete, copy, and comments. Opened through `ModalManager`. |
| `src/components/Modals/GroupFormModal.jsx` | Create/edit group modal. | `GroupFormModal` renders group name, member picker, avatar upload, and submit/cancel controls. Calls `handleGroupSubmit` and group form setters from `ChatApp`. Opened through `ModalManager`. |
| `src/components/Modals/GroupSettingsModal.jsx` | Group management modal. | `GroupSettingsModal` edits group name, avatar file, members, admins, and optional deletion/admin actions. Calls `onGroupUpdate`, `setGroupSettingsData`, and related parent callbacks. Opened through `ModalManager`. |
| `src/components/Modals/ModalManager.jsx` | Central modal switchboard. | `ModalManager` receives `activeModal` and the full callback/data prop bundle from `ChatApp`, then renders exactly the matching modal: context menu, profile, group form/settings, task trail/convert/analytics, reminder, schedule send, admin edit, active schedules, or upload overlay. |
| `src/components/Modals/ProfileSettingsModal.jsx` | User profile/settings modal. | `ProfileSettingsModal` renders name, avatar upload, sound profile, and tool preference toggles. Calls `handleProfileSubmit` and profile setters from `ChatApp`. Opened through `ModalManager`. |
| `src/components/Modals/ReminderModal.jsx` | Reminder creation modal. | `ReminderModal` captures reminder datetime and calls parent `setReminder`. Opened through `ModalManager` for a selected message/task. |
| `src/components/Modals/ScheduleSendModal.jsx` | Schedule message/task modal. | `ScheduleSendModal` captures scheduled datetime, previews pending text, and calls `handleScheduleMessage`. Opened through `ModalManager`. |
| `src/components/Modals/TaskAnalyticsModal.jsx` | Compact task analytics modal. | `TaskAnalyticsModal` displays totals for tasks, completed tasks, overdue tasks, and completion rate based on `analyticsData` from `ChatApp`. Opened through `ModalManager`. |
| `src/components/Modals/TaskConvertModal.jsx` | Convert message to task modal. | `TaskConvertModal` manages local `escalationMode` and renders controls for title, assignees, priority, deadline, acknowledgement, proof, and schedule options. Calls `convertToTask` or scheduling callback from `ChatApp`. |
| `src/components/Modals/TaskTrailModal.jsx` | Full task detail/trail modal. | `TaskTrailModal` manages comment/edit/delegate UI state, shows task title, priority, deadline, assignees, acknowledgments, proof, and chronological trail. Calls parent handlers for save title, delegate, complete, comment, and file upload. |
| `src/components/Sidebar/LeftSidebar.jsx` | Main navigation sidebar. | `LeftSidebar` lists current user info, search, groups, DMs, unread counts, pending task counts, admin/task-board switches, create group, profile, and logout controls. Uses `MemoizedAvatar`; receives unread calculators from `ChatApp`. |
| `src/components/Sidebar/RightSidebar.jsx` | Context/details sidebar. | `RightSidebar` shows active group/member details, pinned messages, task lists, and optional `TaskBoard`/admin-related views. Uses `useMemo` for derived task lists and `MemoizedAvatar` for members. Controlled by `ChatApp`. |
| `src/components/Sidebar/TaskBoard.jsx` | Kanban-style task board. | `TaskCard` renders each task card with group, priority/status, deadline, and assignees. `TaskBoard` derives assigned tasks with `useMemo`, groups them into To Do/In Progress/Completed/Overdue, and opens the source conversation via `handleTaskClick`. `renderColumn` renders each board column. |
| `src/hooks/useChatEngine.js` | Chat data and message persistence hook. | `useChatEngine` subscribes to messages/typing, marks seen messages, manages offline drafts in IndexedDB, and exposes DB actions to `ChatApp`. Important helpers: `playAlertSound`, `openDraftDB`, `loadOfflineDrafts`, `saveOfflineDraft`, `deleteOfflineDraft`, `flushOfflineDrafts`, `logImmutableAction`, `triggerTypingEvent`, `sendMessageToDB`, `reactToMessageDB`, `uploadAndSendFileDB`, `scheduleMessageDB`, `editMessageDB`, `deleteMessageDB`, `togglePinDB`, and `toggleBookmarkDB`. Uses Firestore and Storage plus `compressImage`. |
| `src/hooks/useWorkspaceData.js` | Workspace/user/group subscription hook. | `useWorkspaceData` loads current user profile, users, groups, reminders, notifications, tags, announcements, audit logs, and admin state for `ChatApp`. `verifyAdminStatus` checks token claims and VIP admin email. Maintains online heartbeat with Firestore `lastActive`. |
| `src/utils/helpers.js` | Shared formatting and utility helpers. | `lockExtension(originalName, newName)` keeps uploaded file extensions stable. `toSentenceCase(str)` normalizes display text. `formatMessageText(text)` linkifies URLs and preserves line breaks. `EMOJI_LIST` feeds emoji picker. `getNextWorkingDay9AM(fromDate)` defaults reminders/schedules to the next working day morning. |
| `src/utils/imageUtils.js` | Active image compression helper. | `compressImage(file, maxWidth, maxHeight, quality)` uses `FileReader`, `Image`, and canvas resizing to return a compressed JPEG `Blob`. Used by `useChatEngine.uploadAndSendFileDB` before uploading chat images to Firebase Storage. |
