{
  "rules": {
    "typing": {
      "$orgId": {
        ".read": "auth != null",
        "$chatId": {
          "$uid": {
            ".write": "auth != null && auth.uid == $uid",
            ".validate": "newData.hasChildren(['orgId', 'groupId', 'userId', 'name', 'clientTimestamp']) && newData.child('orgId').val() == $orgId && newData.child('userId').val() == auth.uid && newData.child('groupId').isString() && newData.child('name').isString() && newData.child('clientTimestamp').isNumber()"
          }
        }
      }
    },
    ".read": false,
    ".write": false
  }
}
